{  
  "name": "chrode",
  "description": "Chroma for Node",
  "version": "0.0.1",
  "private": true,
  "main": "index.js",
  "scripts": {
    "configure": "node-gyp configure",
    "build": "node-gyp build"
  },
  "dependencies": {
    "bindings": "^1.2.1"
  },
  "devDependencies": {
    "nan": "^2.1.0",
    "node-gyp": "^3.2.0"
  }
}
